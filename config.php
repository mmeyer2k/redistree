<?php

return [
    /*
     * An array of characters to use as default virtual "directory" separators.
     */
    'separators' => ['\\', '/', ':', '|', '+', '#'],
    /*
     * Do not load each key's data when browsing.
     */
    'view_keys_only' => false,
    /*
     * Determine whether to prompt user when performing dangerous actions.
     */
    'danger_prompt' => true,
    /*
     * Show tooltips over interface items.
     */
    'tooltips' => true,
    /*
     * Readonly mode which prevents state changes to database
     */
    'readonly' => false,
    /*
     * Number of keys per page (valid integers: 25, 50, 100, 250, 500, 1000)
     */
    'pagination' => 250,
];
